# ------------------------------------------------------------------
# Maven
# ------------------------------------------------------------------
target/
.mvn/timing.properties
.mvn/wrapper/maven-wrapper.jar

# ------------------------------------------------------------------
# IDE
# ------------------------------------------------------------------
.idea/
*.iml
*.ipr
*.iws
.vscode/
.settings/
.project
.classpath
.factorypath

# ------------------------------------------------------------------
# OS
# ------------------------------------------------------------------
.DS_Store
Thumbs.db

# ------------------------------------------------------------------
# Logs
# ------------------------------------------------------------------
logs/*.log
logs/*.log.gz
!logs/.gitkeep

# Root-level ad-hoc log captures (e.g. manual adb logcat redirects)
/*.log

# ------------------------------------------------------------------
# Reports (regenerated on every run)
# ------------------------------------------------------------------
test-output/
allure-results/
allure-report/
extent-reports/
surefire-reports/

# ------------------------------------------------------------------
# Appium / device artifacts
# ------------------------------------------------------------------
*.apk
*.aab
*.ipa
*.app
appium.log

# ------------------------------------------------------------------
# Build storage (app binaries are never committed; keep folder structure)
# ------------------------------------------------------------------
builds/**/*.apk
builds/**/*.aab
builds/**/*.ipa
!builds/**/.gitkeep

# ------------------------------------------------------------------
# Env / secrets
# ------------------------------------------------------------------
.env
*.local.properties

# ------------------------------------------------------------------
# Generated authentication storage state (see StorageStateManager —
# now generated under target/generated-test-resources on every suite
# run, already covered by the top-level target/ rule above). These
# entries guard the old src/test/resources location against ever
# being repopulated, and cover any manual backups made there.
# ------------------------------------------------------------------
src/test/resources/**/storageStates/user.xml
src/test/resources/**/storageStates/*.bak*
