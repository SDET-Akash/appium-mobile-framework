package com.automation.mobile.reports;

/**
 * Helper for Allure-specific reporting concerns not covered by the
 * {@code allure-testng} adapter alone — e.g. attaching screenshots/logs
 * to the current Allure step, or adding environment/execution metadata
 * to the generated report.
 * <p>
 * Implementation intentionally deferred to a later iteration.
 */
public class AllureManager {
}
