package com.automation.mobile.listeners;

/**
 * TestNG {@code IAnnotationTransformer} implementation used to
 * programmatically apply cross-cutting {@code @Test} annotation
 * behavior at runtime — most notably wiring {@link RetryAnalyzer} onto
 * every test method without requiring it on each {@code @Test}
 * annotation individually.
 * <p>
 * Implementation intentionally deferred to a later iteration.
 */
public class AnnotationTransformer {
}
