package com.automation.mobile.listeners;

/**
 * TestNG {@code ITestListener} implementation that hooks into test
 * lifecycle events (start/success/failure/skip) to drive cross-cutting
 * concerns such as logging, screenshot capture on failure, and
 * report/listener notifications (Extent, Allure).
 * <p>
 * Implementation intentionally deferred to a later iteration.
 */
public class TestListener {
}
