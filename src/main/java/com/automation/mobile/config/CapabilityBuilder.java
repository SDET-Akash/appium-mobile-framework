package com.automation.mobile.config;

/**
 * Builds platform-specific Appium desired capabilities / {@code UiAutomator2Options}
 * / {@code XCUITestOptions} objects from resolved {@link ConfigManager} values,
 * so capability assembly logic lives in one place instead of being duplicated
 * across driver factories.
 * <p>
 * Implementation intentionally deferred to a later iteration.
 */
public class CapabilityBuilder {
}
