package com.automation.mobile.config;

/**
 * Enumerates the execution environments the framework can target (e.g.
 * QA, DEV, STAGING), used by {@link ConfigManager} to select which
 * environment-specific configuration source to load.
 * <p>
 * Constants intentionally deferred to a later iteration.
 */
public enum Environment {
}
