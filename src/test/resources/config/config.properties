# ------------------------------------------------------------------
# Environment / platform configuration placeholder.
# Actual keys (appium server URL, device capabilities, app path, etc.)
# will be populated when the ConfigManager / DriverFactory layer is built.
# ------------------------------------------------------------------
environment=qa
platform=android
