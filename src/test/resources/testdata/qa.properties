# Dummy placeholders only — real credentials are loaded from the
# untracked .env.qa file at the project root (see UserDataReader).
# These values are never used by the framework.
validUserEmail=dummy.valid.user@example.invalid
validUserPassword=REPLACE_WITH_REAL_VALUE_IN_ENV_QA
invalidUserEmail=dummy.invalid.user@example.invalid
invalidUserPassword=REPLACE_WITH_REAL_VALUE_IN_ENV_QA
